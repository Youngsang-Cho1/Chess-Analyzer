services:
  chess-analyzer:
    build: .
    container_name: chess-analyzer
    ports:
      - "8000:8000"
    volumes:
      # auto reload
      - ./app:/app
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - GROQ_API_KEY=${GROQ_API_KEY}
    # command: python main.py
    command: sleep infinity

  db:
    image: postgres:15
    container_name: chess-analyzer-db
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data

  dashboard:
    build: .
    container_name: chess-analyzer-dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./app:/app
    environment:
      - PYTHONUNBUFFERED=1
      - GROQ_API_KEY=${GROQ_API_KEY}
    command: streamlit run dashboard.py --server.address=0.0.0.0
    depends_on:
      - db
    env_file:
      - .env

volumes:
  postgres_data:
